const radiusOutput = document.qetElementById('radius');
const areaOutput = document.querySelector('area');

let area = 0;
const PI = 3.14159;

const radius = 0;
area = PI * radius * radius;
radiusOutput = radius;
areaOutput = area;

radius = 20;
area = PI * radius * radius;
radiusOutput = radius;
areaOutput = area;